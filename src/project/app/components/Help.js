var React = require('react');


module.exports = React.createClass({
    render: function() {
    	return <div className="page-wrapper">
            <img src="/images/help.png" />
            <br />
            <p>This could be a help page... for now just to demo the routing.</p>
        </div>
    }
});
